pipeline {
    agent any

    stages {
        stage('Pull') {
            steps {
                sh 'docker pull prathikm/marineni'          
            }
        }
        stage('Run') {
            steps {
                sh 'docker run -d --name prathik -p 3000:5000 prathikm/marineni'
            }
        }
    }
}
