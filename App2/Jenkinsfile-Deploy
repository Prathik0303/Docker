pipeline {
    agent any

    stages {
        stage('Pull') {
            steps {
                sh 'docker pull prathikm/marineni'          
            }
        }
        stage('Run') {
            steps {
                sh 'docker run -d --name marineni -p 3000:5000 prathikm/marineni'
            }
        }
    }
}
